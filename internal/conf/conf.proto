syntax = "proto3";

package internal.conf;

import "google/protobuf/duration.proto";
import "config/config.proto";

option go_package = "github.com/aide-family/rabbit/internal/conf;conf";

message Bootstrap {
	Environment environment = 1;
	Server server = 2;
	repeated rabbit.config.Cluster clusters = 3;
	GlobalEmail globalEmail = 4;
	JWT jwt = 5;
	rabbit.config.ETCD etcd = 6;
	map<string, rabbit.config.MySQL> biz = 7;
	rabbit.config.MySQL main = 8;
	repeated string namespaceAllowList = 9;
}

enum Environment {
	UNKNOWN = 0;
	DEV = 1;
	TEST = 2;
	PREVIEW = 3;
	PROD = 4;
}

message Server {
	message HTTPServer {
		string address = 1;
		string network = 2;
		google.protobuf.Duration timeout = 3;
	}
	
	message GRPCServer {
		string address = 1;
		string network = 2;
		google.protobuf.Duration timeout = 3;
	}
	
	string name = 1;
	map<string, string> metadata = 2;
	HTTPServer http = 3;
	GRPCServer grpc = 4;
}

message GlobalEmail {
	string host = 1;
	int32 port = 2;
	string username = 3;
	string password = 4;
}

message JWT {
	string secret = 1;
	repeated string allowList = 2;
	google.protobuf.Duration expire = 3;
	string issuer = 4;
}