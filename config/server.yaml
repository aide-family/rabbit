environment: ${MOON_rabbit_ENVIRONMENT:PROD}
enableSwagger: ${MOON_rabbit_ENABLE_SWAGGER:true}
enableMetrics: ${MOON_rabbit_ENABLE_METRICS:true}
useRandomID: ${MOON_rabbit_USE_RANDOM_NODE_ID:false}

server:
  name: ${MOON_rabbit_SERVER_NAME:rabbit}
  metadata:
    tag: ${MOON_rabbit_METADATA_TAG:rabbit}
    repository: ${MOON_rabbit_METADATA_REPOSITORY:https://github.com/aide-family/rabbit}
    author: ${MOON_rabbit_METADATA_AUTHOR:Aide Family}
    email: ${MOON_rabbit_METADATA_EMAIL:aidecloud@163.com}
  http:
    address: "${MOON_rabbit_HTTP_ADDRESS:0.0.0.0:18080}"
    network: "${MOON_rabbit_HTTP_NETWORK:tcp}"
    timeout: "${MOON_rabbit_HTTP_TIMEOUT:10s}"
    protocol: HTTP
  grpc:
    address: "${MOON_rabbit_GRPC_ADDRESS:0.0.0.0:19090}"
    network: "${MOON_rabbit_GRPC_NETWORK:tcp}"
    timeout: "${MOON_rabbit_GRPC_TIMEOUT:10s}"
    protocol: GRPC

jwt:
  secret: "${MOON_rabbit_JWT_SECRET:xxx}"
  expire: "${MOON_rabbit_JWT_EXPIRE:600s}"
  issuer: "${MOON_rabbit_JWT_ISSUER:rabbit}"

namespaceConfig:
  driver: ${MOON_rabbit_NAMESPACE_DRIVER:GORM}
  version: ${MOON_rabbit_NAMESPACE_VERSION:v1}
  options:
    '@type': "${MOON_rabbit_NAMESPACE_OPTIONS_TYPE:type.googleapis.com/rabbit.config.ORMConfig}"
    dialector: ${MOON_rabbit_NAMESPACE_DIALECTOR:SQLITE}
    debug: ${MOON_rabbit_NAMESPACE_DEBUG:true}
    useSystemLogger: ${MOON_rabbit_NAMESPACE_USE_SYSTEM_LOGGER:true}
    options:
      '@type': "${MOON_rabbit_NAMESPACE_OPTIONS_TYPE:type.googleapis.com/rabbit.config.SQLiteOptions}"
      dsn: "${MOON_rabbit_NAMESPACE_SQLITE_OPTIONS_DSN:file:./rabbit.db?cache=shared}"

loginConfig:
  driver: ${MOON_rabbit_LOGIN_DRIVER:GORM}
  version: ${MOON_rabbit_LOGIN_VERSION:v1}
  options:
    '@type': "${MOON_rabbit_LOGIN_OPTIONS_TYPE:type.googleapis.com/rabbit.config.ORMConfig}"
    dialector: ${MOON_rabbit_LOGIN_DIALECTOR:SQLITE}
    debug: ${MOON_rabbit_LOGIN_DEBUG:true}
    useSystemLogger: ${MOON_rabbit_LOGIN_USE_SYSTEM_LOGGER:true}
    options:
      '@type': "${MOON_rabbit_LOGIN_OPTIONS_TYPE:type.googleapis.com/rabbit.config.SQLiteOptions}"
      dsn: "${MOON_rabbit_LOGIN_SQLITE_OPTIONS_DSN:file:./rabbit.db?cache=shared}"

swaggerBasicAuth:
  enabled: ${MOON_rabbit_SWAGGER_BASIC_AUTH_ENABLED:false}  
  username: ${MOON_rabbit_SWAGGER_BASIC_AUTH_USERNAME:moon.rabbit}
  password: ${MOON_rabbit_SWAGGER_BASIC_AUTH_PASSWORD:rabbit.swagger}

metricsBasicAuth:
  enabled: ${MOON_rabbit_METRICS_BASIC_AUTH_ENABLED:false}
  username: ${MOON_rabbit_METRICS_BASIC_AUTH_USERNAME:moon.rabbit}
  password: ${MOON_rabbit_METRICS_BASIC_AUTH_PASSWORD:rabbit.metrics}

oauth2:
  enable: ${MOON_rabbit_OAUTH2_ENABLE:true}
  redirectUri: ${MOON_rabbit_OAUTH2_REDIRECT_URI:http://localhost:18080/oauth2/callback}
  configs:
    - app: ${MOON_rabbit_OAUTH2_CONFIGS_APP:GITHUB}
      clientId: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_ID:github.client.id}
      clientSecret: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_SECRET:github.client.secret}
      callbackUri: ${MOON_rabbit_OAUTH2_CONFIGS_CALLBACK_URI:http://localhost:18080/oauth2/callback}
      authUrl: ${MOON_rabbit_OAUTH2_CONFIGS_AUTH_URL:https://github.com/login/oauth/authorize}
      tokenUrl: ${MOON_rabbit_OAUTH2_CONFIGS_TOKEN_URL:https://github.com/login/oauth/access_token}
      scopes:
        - user:email
      loginUrl: ${MOON_rabbit_OAUTH2_CONFIGS_LOGIN_URL:https://github.com/login/oauth/authorize}
    - app: ${MOON_rabbit_OAUTH2_CONFIGS_APP:GITEE}
      clientId: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_ID:gitee.client.id}
      clientSecret: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_SECRET:gitee.client.secret}
      callbackUri: ${MOON_rabbit_OAUTH2_CONFIGS_CALLBACK_URI:http://localhost:18080/oauth2/callback}
      authUrl: ${MOON_rabbit_OAUTH2_CONFIGS_AUTH_URL:https://gitee.com/oauth/authorize}
      tokenUrl: ${MOON_rabbit_OAUTH2_CONFIGS_TOKEN_URL:https://gitee.com/oauth/access_token}
      scopes:
        - user:email
      loginUrl: ${MOON_rabbit_OAUTH2_CONFIGS_LOGIN_URL:https://gitee.com/oauth/authorize}
    - app: ${MOON_rabbit_OAUTH2_CONFIGS_APP:FEISHU}
      clientId: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_ID:feishu.client.id}
      clientSecret: ${MOON_rabbit_OAUTH2_CONFIGS_CLIENT_SECRET:feishu.client.secret}
      callbackUri: ${MOON_rabbit_OAUTH2_CONFIGS_CALLBACK_URI:http://localhost:18080/oauth2/callback}
      authUrl: ${MOON_rabbit_OAUTH2_CONFIGS_AUTH_URL:https://open.feishu.cn/open-apis/authen/v1/authorize}
      tokenUrl: ${MOON_rabbit_OAUTH2_CONFIGS_TOKEN_URL:https://open.feishu.cn/open-apis/authen/v1/access_token}
      scopes:
        - user:email
      loginUrl: ${MOON_rabbit_OAUTH2_CONFIGS_LOGIN_URL:https://open.feishu.cn/open-apis/authen/v1/authorize}